# Node / Vite
node_modules/
dist/
.vite/
*.tsbuildinfo

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Rust build output (only matters if someone rebuilds WASM locally)
wasm-src/target/
wasm-src/Cargo.lock.bak

# Vercel artifacts (this repo deploys via GitHub Pages)
.vercel/
